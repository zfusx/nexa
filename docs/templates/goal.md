### `<goal-id>`: `<objective>`

| Field | Value |
| --- | --- |
| Priority | `<P0/P1/P2>` |
| Owner | `<owner>` |
| Status | `<proposed | active | blocked | completed | canceled>` |
| Started | `<YYYY-MM-DD>` |
| Completed | `<YYYY-MM-DD-or-blank>` |
| Related tasks | `<task-ids-or-links>` |
| Related decisions | `<decision-ids-or-links>` |
| Related research | `<research-ids-or-links>` |

#### Problem

`<what problem this goal solves>`

#### Target Outcome

`<what should be true when this goal is complete>`

#### Scope

In scope:

- `<scope-item>`

Out of scope:

- `<non-goal>`

#### Acceptance Criteria

- `<observable-criterion>`
- `<verification-or-evidence-required>`

#### Plan

1. `<step>`
2. `<step>`
3. `<step>`

#### Verification

| Check | Command or Evidence | Required | Result |
| --- | --- | --- | --- |
| `<check>` | `<command-or-link>` | `<yes/no>` | `<pending/pass/fail/skipped>` |

#### Completion Notes

`<summary-of-result-and-any-follow-up>`
