# 部署说明

## v0.2 发布记录（2026-09-14）

用户已授权继续完成整合发布。发布内容为七页静态演示、设计参考与白皮书审阅稿，不包含真实 API、账户或支付。

- 新 release：`/var/www/main.zfis.net/releases/20260914-180159`。
- 切换前当前 release 已只读核实为 `20260914-172141`，作为本次直接回滚目标；更早的 `20260914-171851` 继续保留。
- 仅上传本地 `dist/`；逐文件 SHA256 校验后原子切换软链接，不改 Caddy 配置，不重启 Nginx。
- 切换后检查七路由、PDF、资源与未知路由404，浏览器完成关键体验；失败立刻切回 `20260914-172141`。
- 团队源码 ZIP 留在本地交付，不上传公开服务器。正式入口未知，演示 CTA 留在站内。

执行结果：已核实旧 release 后上传；14 个文件在远端磁盘和 HTTPS 返回内容均与本地 SHA256 一致，七页及资源均为 200，PDF 类型正确，指纹资源为 immutable，未知路径 404。线上三题交互与手机结果呈现通过。Caddy 配置未变更，旧 release 未删除。

## 当前状态

| 项目         | 内容                                              |
| ------------ | ------------------------------------------------- |
| 状态         | 已部署并通过验收                                  |
| 目标域名     | `https://main.zfis.net`                           |
| 目标主机     | `main.zfis.net`，SSH 端口 2028                    |
| 实际运行方式 | Astro 静态文件 + Caddy 2                          |
| 当前 release | `/var/www/main.zfis.net/releases/20260915-011248` |
| 回滚 release | `/var/www/main.zfis.net/releases/20260914-234320` |
| 入口软链接   | `/var/www/main.zfis.net/current`                  |
| Caddy 配置   | `/etc/caddy/sites-enabled/main.zfis.net.caddy`    |
| 主配置备份   | `/etc/caddy/Caddyfile.pre-nexa-20260914-171851`   |

## 部署前只读检查

- DNS、TLS 与当前 HTTP 响应。
- 实际反向代理配置位置和语法（本机为 Caddy；不要启动 Nginx）。
- 当前站点根目录、文件所有者和磁盘空间。
- 是否已有服务、容器、静态文件或重要配置。
- 可恢复备份目录和回滚命令。

## 后续发布流程

1. 本地执行格式、类型、测试、构建、链接和秘密扫描。
2. 新建 `/var/www/main.zfis.net/releases/<timestamp>` 并上传 `dist/`。
3. 在 release 内检查首页、子页和静态资源存在。
4. 创建临时软链接并用 `mv -Tf` 原子切换 `current`。
5. 验证 HTTPS、首页、子页面、404、交互与静态资源。
6. 失败时立刻把 `current` 切回上一 release。

## 当前回滚命令

```bash
ssh root@main.zfis.net -p 2028
test ! -e /var/www/main.zfis.net/current.rollback-20260915-011248 && test ! -L /var/www/main.zfis.net/current.rollback-20260915-011248 && ln -s releases/20260914-234320 /var/www/main.zfis.net/current.rollback-20260915-011248 && mv -Tf /var/www/main.zfis.net/current.rollback-20260915-011248 /var/www/main.zfis.net/current
```

Caddy 的 `root` 始终指向 `current`，仅切换静态目录不需要重载服务。若需要撤销首次 Caddy 接入，应先离线验证备份配置，再恢复 `/etc/caddy/Caddyfile.pre-nexa-20260914-171851` 并 reload。

回滚前先核实当前仍为本记录的新 release，且目标目录存在；如已发生后续发布，应使用最新发布记录，不执行过期回滚命令。

## 文案更新记录（2026-09-14 18:19）

首页主标题由“把命运看清一点，把选择留给自己”更新为“对命运好奇，不等于把人生交给命运”，并同步首屏说明、故事段落和页面元信息。新 release `20260914-181900` 从已验收的 `20260914-180159` 创建；14 个线上文件与本地构建内容一致，未知路径 404，桌面和手机首屏通过视觉检查。上一版保留为直接回滚目标。

## 白皮书专业版发布记录（2026-09-14 18:35）

`/whitepaper` 与下载 PDF 升级为 v0.2 专业数学版。发布前只读核实当前为 `20260914-181900`；新 release `20260914-183431` 仅含本地 `dist/` 的 14 个公开文件。上传后 checksum dry-run 无差异，再原子切换 `current`。线上 14 个文件与本地 SHA256 逐一一致，PDF 返回 `application/pdf`，页面展示 v0.2，未知路径 404，Caddy active，桌面视觉与浏览器控制台通过。旧版 `20260914-181900` 保留为直接回滚目标。

## 双版本白皮书发布记录（2026-09-14 18:43）

`/whitepaper` 增加 v0.1 普通读者版与 v0.2 专业数学版的并列入口，当前版仍以 v0.2 为主。发布前只读核实当前为 `20260914-183431`；新 release `20260914-184229` 含 15 个公开文件和两份指纹 PDF。checksum dry-run 无内容差异后原子切换。线上 15 个文件与本地 SHA256 逐一一致，两份 PDF 均返回 `application/pdf`，未知路径 404，Caddy active，桌面/手机视觉与控制台通过。旧版 `20260914-183431` 保留为直接回滚目标。

## 独立 30 题引流体验发布记录（2026-09-14 19:08）

新增 `/quiz` 独立落地页、首页入口、30 道题浏览器内交互和免费五维结果。发布前只读核实当前为 `20260914-184229`；新 release `20260914-190750` 含 18 个公开文件。上传后 checksum dry-run 无差异，再原子切换 `current`。线上 18 个文件与本地 SHA256 逐一一致，`/quiz` 为 200、未知路径 404、Caddy active；桌面/390px 手机流程和线上浏览器控制台通过。旧版 `20260914-184229` 保留为直接回滚目标。

## AskNexa 视觉统一发布记录（2026-09-14 21:40）

八条路由统一为 asknexa.me 的深紫表面、Inter/中文无衬线、紫红主动作和克制卡片；30 题结果改为五色发光柱状图，两版白皮书 PDF 内容与排版未修改。第一轮视觉 release `20260914-213548` 从 `20260914-190750` 切换；根据 dev.asknexa.me 功能页继续校准按钮、选项和图表后，切换到 `20260914-214043`。20 个公开文件逐一 HTTPS SHA256 一致，未知路径 404，Caddy active；桌面/390px 手机及完整 30 题结果通过。

## 深层页面与 SVG 规范发布记录（2026-09-14 21:57）

进一步核对月运、年运、六爻结果/历史、命书、八字表、通知、语言、帮助、社交、法律中心等页面；在线 `/design-system` 新增十个重新绘制的 SVG 图标及桌面/手机规范，不复制或热链测试站文件。发布前只读核实当前为 `20260914-214043`；新 release `20260914-215714` 上传后 checksum dry-run 无差异，再原子切换。线上 21 个文件与本地 SHA256 一致，未知路径 404、Caddy active；390px 下图标三列、页面无横向溢出，浏览器控制台无警告或错误。`20260914-214043` 保留为直接回滚目标。

## Dev 同源品牌视觉 v0.5 发布记录（2026-09-14 23:36）

根据二次 computed-style 色差复核，全站切换为 Dev 的冷紫四级表面和三档文字，并本地化产品站 App mark、星芒 SVG、橙蓝几何插画与年运顶部图。发布前只读核实当前为 `20260914-215714`、Caddy 配置有效且有 17G 可用空间；新 release `20260914-233320` 上传后 checksum dry-run 无差异，再原子切换。

26 个线上文件的远端磁盘与 HTTPS SHA256 全部与本地一致；八页、PNG/WebP/SVG 品牌资产类型、未知路径 404 和 Caddy active 通过。线上 1440px 首页/白皮书、390px 首页/设计参考完成视觉检查，浏览器控制台无警告或错误。

交付收尾时发现白皮书引用块仍保留一处旧灰紫 `#211849`，已改用统一的 `#21124B` 主卡令牌。发布前只读确认线上仍为 `20260914-233320`、Caddy 有效且旧版目录存在；跟进 release `20260914-234320` 逐文件上传并原子切换。26 个 HTTPS 文件 SHA256 零差异，八路由 200、未知路径 404、品牌资源类型与 Caddy 状态通过，真实浏览器再次复核首页和白皮书。`20260914-233320` 保留为直接回滚目标。

## 全局导航短标题发布记录（2026-09-15 01:15）

桌面导航改为“体验 / 测评 / 原理 / 关于 / 白皮书 / 开始体验”，页脚同步使用“测评”，不改变页面路由和首页正文。发布前只读核实当前为 `20260914-234320` 且 Caddy 配置有效；新 release `20260915-011248` 上传校验后原子切换。26 个 HTTPS 文件 SHA256 全部与本地一致，八路由 200、未知路径 404、Caddy active。`20260914-234320` 保留为直接回滚目标。

## 禁止事项

- 不直接覆盖未知目录。
- 不在命令或文档中记录 SSH 私钥、密码或令牌。
- 不在没有备份和回滚命令时切换线上流量。
- 不删除当前与上一版 release，直到下一次发布稳定。
