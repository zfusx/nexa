# API 契约

## 当前状态

演示站不调用生产 API，不上传测试答案，不收集精确出生资料，也不实现登录或支付。

## 浏览器内演示数据

轻量测试的输入与结果只保存在页面运行内存中；刷新页面即清除。结果必须明确标注为“产品体验示例”，不能伪装成真实八字或易经计算。

## 将来接入原则

正式接入 Nexa 服务前，必须先补充：API 基础地址与版本；认证、授权和速率限制；出生资料和对话数据的字段、用途、保存期限与删除方法；错误格式、超时和降级行为；前端埋点事件与同意机制；测试与生产环境隔离。

在上述契约完成并获得审阅前，演示站不得向未知端点发送用户数据。

## v0.2 团队对接材料

当前本地函数契约、真实服务所需能力、错误/任务语义及接入门槛见 [实现与服务接入说明](docs/engineering/实现与服务接入说明.md)。该文件是协商清单，不是已冻结的 HTTP schema。

产品入口统一在 `src/lib/site.ts`。正式 URL 未确认前，CTA 使用站内方法页并明确不提供注册付款，不再将普通访客导向开发环境。未经签认的域内八字字段不在主站重新命名。
