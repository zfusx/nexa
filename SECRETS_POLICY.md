# 密钥与敏感信息规范

## 当前清单

项目当前没有应用运行密钥。演示站必须保持纯静态，不需要 API Key、数据库密码或支付密钥。

SSH 凭据只能使用用户本机已有的 SSH 配置、代理或系统密钥链；不得复制进仓库、聊天输出、日志或部署脚本。

## 规则

- `.env` 和所有私钥格式必须被 Git 忽略。
- 文档只记录变量名称，不记录值。
- 不把 `white_paper/` 中的商业机密发布到站点构建目录。
- 不把原始录屏、用户资料或内部评测数据上传到公开站点。
- 部署和配置变更后运行 Gitleaks；工具不可用时记录原因并使用替代检查。

## 当前合规状态

| 项目               | 状态                                                    |
| ------------------ | ------------------------------------------------------- |
| 代码中无已知密钥   | 基础敏感模式扫描未发现；不等同于完整安全审计            |
| `.env` 已忽略      | compliant                                               |
| 生产密钥存储方式   | 不适用；静态演示站                                      |
| SSH 凭据未进入仓库 | 仅使用本机已有 SSH；未复制凭据到源码或发布目录          |
| Gitleaks           | 本机未安装，skipped；已做替代定向检查                   |
| 发布/交付范围      | 站点扩展名白名单与 ZIP 文件白名单校验通过；内部目录排除 |
